<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.douzone.mapper.user">

    <select id="selectUserAll" resultType="com.douzone.final_backend.User.UserBean">
        select * from user
    </select>

    <select id="existsById" resultType="boolean">
        select count(*) from user where u_id = #{u_id}
    </select>

    <select id="findByIdAndPassword" resultType="com.douzone.final_backend.User.UserBean">
        select * from user where u_id = #{u_id} and u_pw = #{u_pw}
    </select>
</mapper>